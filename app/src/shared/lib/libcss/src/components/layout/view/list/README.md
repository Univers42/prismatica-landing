# list/

🚧 **placeholder** — future list view layout.

reserved for a data list view — think Notion's list view or a structured table alternative with rows and configurable columns.

## what it will probably include

- sortable/filterable rows
- column configuration
- row selection (single/multi)
- inline editing
- virtual scrolling for large datasets

## see also

- [../](../) — parent view directory
- the `Table` atom handles simple static tables, this would be the interactive version
