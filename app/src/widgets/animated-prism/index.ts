/**
 * 🧱 Public API: Animated Prism
 * 
 * Exposes the declarative AnimatedPrism wrapper.
 * External consumers must not access the Canvas engine or internal hooks directly.
 */
export { AnimatedPrism } from './ui/AnimatedPrism';
