/**
 * 🧱 Public API: Hero Section
 * 
 * Exposes the declarative HeroSection wrapper.
 * External consumers must not access the internal logic engines or physical DOM constraints.
 */
export { HeroSection } from './ui/HeroSection';
