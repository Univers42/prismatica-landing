/**
 * 🧱 Public API: Landing Page
 * 
 * Exposes the LandingPage orchestrator. 
 * External layers (like App) should only import from this file, never directly from `ui/`.
 */
export { LandingPage } from './ui/LandingPage';
